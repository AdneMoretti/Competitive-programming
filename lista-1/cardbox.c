#include <stdio.h>
#include <stdlib.h>

int main(){
    int N, Q;
    int num, a, b, k;
    scanf("%d %d", &N, &Q);
    for(int i=0;i<Q;i++){
        scanf("%d", &num);
        switch (num)
        {
        case 1:
            scanf("%d %d", &a, &b);


        case 2: 
            scanf("%d", &b);

        default:
            break;
        }
    }

}