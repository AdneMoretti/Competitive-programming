#include <iostream> 
#include <bits/stdc++.h>

using namespace std;
using ll = long long;
// daria tle pelo peso de operações
// busca binaria para saber qual o maior pornto que ta ddentro 

ll solve(int R)
{ 
    ll ans = 0;
    for( ll x = -R; x)
        ll a = -R, b = R, ymin = R;
        while(b>=a){
            auto m = a + (b-a)/2
        }
}
int main(){
    ios::sync_with_stdio(false);

    int T;
    cin >> T;

    while(T--)
    {
        int A, B, R;
        cin >> A >> B >> R;
        cout << solve(R) << endl;
    }
}
 // GEOMETRIA COMPUTACIONAL
// poligono arbitratio
// TEOREMA DE PICK
//  A = I + B/2 - 1